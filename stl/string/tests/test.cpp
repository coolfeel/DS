#include <iosteam>
#include <string>

/*
字符串
直接访问 ： str[i]
输出 ： printf("%s", str.c_str()) ，将string型str 使用c_str()变为字符数组 输出
拼接 ： str1 + str2
字典序比较大小 ： ==, <, <=, >, >=, !=
length()/size()
insert(pos, string), 插入
insert(resource_pos, aim_first_pos, aim_last_pos)   eg : insert(str.begin() + 3, str2.begin(), str2.end())
erase(),删除单个或区间
clear()
substr(pos, len),子串， 开始位置及长度
str.find(str2), 找str2子串的第一次出现的位置
replace(pos, len, str2)替换
replace(start, end, str2)


*/



using namespace std;


int main() {

    return 0;
}
