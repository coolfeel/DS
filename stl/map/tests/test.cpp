#include <iostream>
#include <map>


/*
map，可以将任何基本类型 映射到任何基本类型，键值会从小到大的顺序自动排序
访问 : 通过下标访问, m['c']， 迭代器访问,it->first键, it->second值
find(key),返回键为key的迭代器
erase(it),删除单个或区间
erase(key)
erase(first, last)
size()
clear()

key唯一，map
key不唯一可重复， multimap
只对应而不按key排序的,unordered_map

*/




using namespace std;

int main() {

    return 0;
}
